import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev indicator ("N" logo) shown during development.
  devIndicators: false,
};

export default nextConfig;
